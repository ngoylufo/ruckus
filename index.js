import * as globals from "./internal/globals.js";
import { setup, updateWith, renderWith, run, stop } from "./core.js";

export default {
  globals,
  setup,
  updateWith,
  renderWith,
  run,
  stop,
};
